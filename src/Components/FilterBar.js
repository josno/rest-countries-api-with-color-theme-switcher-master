import React from "react";

import styled from "styled-components";

const FilterBar = (props) => {
	return <FilterBarStyles></FilterBarStyles>;
};

const FilterBarStyles = styled.div``;

export default FilterBar;
