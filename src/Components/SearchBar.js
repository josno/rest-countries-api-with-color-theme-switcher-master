import React from "react";

import styled from "styled-components";

const SearchBar = (props) => {
	return <SearchBarStyles></SearchBarStyles>;
};

const SearchBarStyles = styled.div``;

export default SearchBar;
